---
dependency:
  name: galaxy
  enabled: false
driver:
  name: docker
platforms:
  - name: test-instance
    image: alpine:3.18
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
